# Test Database Configuration (H2 In-Memory)
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MYSQL
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Business DataSource Configuration for Tests (same as primary)
business.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MYSQL
business.datasource.driver-class-name=org.h2.Driver
business.datasource.username=sa
business.datasource.password=

# JPA Configuration for H2
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Use our custom schema.sql instead
spring.sql.init.mode=always

# Spring Batch Configuration
spring.batch.job.enabled=false
spring.batch.initialize-schema=always

# Logging
logging.level.org.springframework.batch=DEBUG
logging.level.com.example.batch=DEBUG
